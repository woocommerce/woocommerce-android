<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <corners android:radius="8dp" />

    <padding
        android:bottom="1dp"
        android:left="4dp"
        android:right="4dp"
        android:top="1dp" />

    <size
        android:width="15dp"
        android:height="15dp" />

    <solid android:color="@color/badge_background_color"/>

    <!--
        Use a color resource for dark mode so we can add the elevation alpha
    -->
    <stroke
        android:width="1dp"
        android:color="@color/badge_border_color"/>
</shape>
