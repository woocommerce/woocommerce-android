<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_arrow_up"
        android:icon="@drawable/ic_arrow_up"
        app:showAsAction="always"
        android:visible="false"
        android:title="@string/previous_order" />
    <item
        android:id="@+id/menu_arrow_down"
        android:icon="@drawable/ic_arrow_down"
        app:showAsAction="always"
        android:visible="false"
        android:title="@string/next_order" />
    <item
        android:id="@+id/menu_edit_order"
        android:title="@string/edit"
        android:visible="false"
        app:showAsAction="always" />
</menu>
