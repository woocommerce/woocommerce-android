# Reading the merged manifest means Robolectric now honours android:name, but the generated Hilt_
# superclass the real application class needs is built by hilt* tasks, which are off for unit tests.
application=android.app.Application
