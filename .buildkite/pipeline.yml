# yaml-language-server: $schema=https://raw.githubusercontent.com/buildkite/pipeline-schema/main/schema.json
---

agents:
  queue: "android"

steps:
  - label: "1 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "2 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "3 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "4 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "5 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "6 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "7 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "8 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "9 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

  - label: "10 - 🛠 Assemble with CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build with CC [With Restore]"
      ./gradlew assembleJalapenoDebug
    plugins: [ $CI_TOOLKIT ]

    # ------------------------------------------------------------------------------------------------------------------

  - label: "1 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "2 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "3 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "4 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "5 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "6 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "7 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "8 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "9 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]

  - label: "10 - 🛠 Assemble without CC [With Restore]"
    command: |
      echo "--- :rubygems: Setting up Gems"
      install_gems

      echo "--- :closed_lock_with_key: Installing Secrets"
      bundle exec fastlane run configure_apply

      .buildkite/commands/restore-cache.sh

      echo "--- 🛠 Build without CC [With Restore]"
      ./gradlew assembleJalapenoDebug --no-configuration-cache
    plugins: [ $CI_TOOLKIT ]
