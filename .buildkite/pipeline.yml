# Nodes with values to reuse in the pipeline.
common_params:
  # Common plugin settings to use with the `plugins` key.
  - &common_plugins
    - automattic/a8c-ci-toolkit#2.17.0
  - &common_plugins_with_test_collector
    - automattic/a8c-ci-toolkit#2.17.0
    - test-collector#v1.8.0:
        files: "WooCommerce/build/buildkite-test-analytics/*.xml"
        format: "junit"        

steps:
  - label: "Gradle Wrapper Validation"
    command: |
      validate_gradle_wrapper
    plugins: *common_plugins

  # Wait for Gradle Wrapper to be validated before running any other jobs
  - wait

  - label: "detekt"
    command: |
      echo "--- 🧹 Linting"
      cp gradle.properties-example gradle.properties
      ./gradlew detektAll
    plugins: *common_plugins
    artifact_paths:
      - "**/build/reports/detekt/detekt.html"

  - label: "lint"
    command: |
      echo "--- 🧹 Linting"
      cp gradle.properties-example gradle.properties
      ./gradlew lintJalapenoDebug
    plugins: *common_plugins
    artifact_paths:
      - "**/build/reports/lint-results*.*"

  - label: "Dependency Tree Diff"
    command: |
      cp gradle.properties-example gradle.properties
      .buildkite/commands/dependency-tree-diff.sh
    if: build.pull_request.id != null
    plugins: *common_plugins

  - label: "Unit tests"
    command: .buildkite/commands/run-unit-tests.sh
    plugins: *common_plugins_with_test_collector
    env:
      BUILDKITE_ANALYTICS_TOKEN: $BUILDKITE_ANALYTICS_TOKEN_UNIT_TESTS
    artifact_paths:
      - "**/build/test-results/*/*.xml"

  - label: "Ensure Screenshot Tests Build"
    command: |
      echo "--- ⚒️ Building"
      cp gradle.properties-example gradle.properties
      ./gradlew assembleJalapenoDebugAndroidTest
    plugins: *common_plugins

  - label: "🛠 Installable Build"
    command: ".buildkite/commands/installable-build.sh"
    if: build.pull_request.id != null
    plugins: *common_plugins

  - label: "Instrumented tests"
    command: .buildkite/commands/run-instrumented-tests.sh
    plugins: *common_plugins_with_test_collector
    env:
      BUILDKITE_ANALYTICS_TOKEN: $BUILDKITE_ANALYTICS_TOKEN_INSTRUMENTED_TESTS 
    artifact_paths:
      - "**/build/instrumented-tests/**/*"
